{
  "name": "debt-bot",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "npm install && nodemon ./bin/www",
    "start": "NODE_ENV=prod node ./bin/www",
    "deploy": "npm version patch && git push && git checkout build && git reset --hard main && cp .gitignore.build .gitignore && npm run client:build && git add client/build && git add .gitignore && git commit --amend --no-edit && git push -f && git checkout main"
  },
  "dependencies": {
    "cookie-parser": "~1.4.4",
    "debug": "~2.6.9",
    "dotenv": "^10.0.0",
    "express": "~4.16.1",
    "http-errors": "~1.6.3",
    "mongodb": "^4.1.3",
    "morgan": "~1.9.1"
  },
  "devDependencies": {
    "concurrently": "^6.2.0",
    "config": "^3.3.6"
  }
}
